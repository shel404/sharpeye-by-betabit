import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Turbopack handles .jsx files by default, so no special config needed
  // If you need to use webpack instead, run: pnpm dev --webpack
};

export default nextConfig;
